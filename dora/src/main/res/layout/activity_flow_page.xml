<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.dora.ui.FlowPageActivity">

    <data>

        <variable
            name="v"
            type="com.example.dora.ui.FlowPageActivity" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <dora.widget.DoraTitleBar
            android:id="@+id/titleBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            app:dview_title="@string/common_title" />

        <TextView
            android:id="@+id/tv_page_loop_mode"
            style="@style/ThemeTextNormal"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_margin="10dp" />


        <FrameLayout
            android:id="@+id/fl_flow_container"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="10dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:layout_gravity="bottom|center_horizontal"
                android:padding="5dp">

                <Button
                    android:id="@+id/btn_switch_page_loop_mode"
                    style="@style/ThemeButton.ObsidianBlack"
                    android:text="切换模式" />

                <Button
                    android:id="@+id/btn_next_page"
                    style="@style/ThemeButton.ObsidianBlack"
                    android:click="@{()->v.nextPage()}"
                    android:text="下一页" />
            </LinearLayout>
        </FrameLayout>
    </LinearLayout>

</layout>