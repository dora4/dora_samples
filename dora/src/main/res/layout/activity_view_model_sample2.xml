<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context="com.example.dora.ui.ViewModelSample1Activity">

    <data>

        <variable
            name="vm"
            type="com.example.dora.vm.ViewModelSample2ViewModel" />
    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical">

        <dora.widget.DoraTitleBar
            android:id="@+id/titleBar"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:background="@color/colorPrimary"
            app:dview_title="@string/common_title" />

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical"
            android:padding="10dp">

            <TextView
                android:id="@+id/tvText"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_marginBottom="5dp"
                style="@style/ThemeTextNormal" />

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical"
                android:padding="5dp">

                <Button
                    style="@style/ThemeButton.ObsidianBlack"
                    android:onClick="@{vm.onClick1}"
                    android:text="Button 1" />

                <Button
                    style="@style/ThemeButton.ObsidianBlack"
                    android:onClick="@{vm.onClick2}"
                    android:text="Button 2" />

                <Button
                    style="@style/ThemeButton.ObsidianBlack"
                    android:onClick="@{vm.onClick3}"
                    android:text="Button 3" />
            </LinearLayout>
        </LinearLayout>
    </LinearLayout>

</layout>